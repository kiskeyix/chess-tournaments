<h1>Listing Rules</h1>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Summary</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rules.each do |rule| %>
      <tr>
        <td><%= rule.name %></td>
        <td><%= rule.summary %></td>
        <td><%= rule.body %></td>
        <td><%= link_to 'Show', rule %></td>
        <td><%= link_to 'Edit', edit_rule_path(rule) %></td>
        <td><%= link_to 'Destroy', rule, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if current_user.admin? %>
<div class='row'>
  <div class='col-sm-2'>
    <%= button_to 'New Rule', new_rule_path, class: 'form-control btn btn-primary', method: :get %>
  </div>
</div>
<% end %>

<%= will_paginate @rules, renderer: BootstrapPagination::Rails %>
