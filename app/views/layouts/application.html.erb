<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Chess::Tournaments</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#chess-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Chess::Tournaments", root_path, title: "Home", class: "navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse" id="chess-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <li>
          <form class="navbar-form navbar-left" role="search">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Search">
            </div>
            <button type="submit" class="btn btn-default form-control">Submit</button>
          </form>
          </li>
          <% if user_signed_in? %>
          <li><%= link_to messages_path,
            class: 'glyphicon glyphicon-envelope',
            role: "button", title: "Messages" do %>
            <span class="badge"><%= current_user.unread_messages.size %></span>
          <% end %></li>
          <% if current_user.player.nil? %>
          <li><%= link_to '', new_player_path, class: 'glyphicon glyphicon-bell',
            role: "button", title: "User not associated with player. Click to create." %>
          </li>
          <% end %>
          <li class="dropdown"> 
            <%- uimg = '' %>
            <%- if current_user.player and current_user.player.image > '' %>
              <% uimg = image_tag(current_user.player.image, size: "32x32") %>
            <%- elsif current_user.image > '' %>
              <% uimg = image_tag(current_user.image, size: "32x32") %>
            <%- end %>
            <%= link_to uimg + " " + current_user.full_name + " > ",'#',
              class: "dropdown-toggle" + ( uimg > '' ? '' : "glyphicon glyphicon-user" ),
              data: { toggle: "dropdown" },
              role: "button"
            %> 
          <ul class="dropdown-menu" role="menu">
              <% if current_user.identities.size < 1 %>
                <%# only local users are allowed to have a password that they know %>
                <li> <%= link_to ' Preferences', edit_user_registration_path %></li>
                <li> <%= link_to ' Edit Player', edit_player_path(current_user.player) %></li>
              <% else %>
                <li> <%= link_to ' Preferences', edit_player_path(current_user.player) %></li>
              <% end %>
              <li>
              <%= link_to ' Sign Out',
                destroy_user_session_path,
                method: :delete,
                class: "glyphicon glyphicon-log-out",
                title: "Terminate session"
              %> 
              </li>
            </ul>
          </li>
        <% else %>
          <li class="dropdown">
          <%= link_to "Sign In > ",'#',
              class: "dropdown-toggle", data: { toggle: "dropdown" },
              role: "button"
             %>
           <ul class="dropdown-menu" role="menu">
             <li><%= link_to 'Sign In with local account',
               new_user_session_path %></li>
             <li><%= link_to 'Create local account',
               new_user_registration_path %></li>
             <hr />
          <%- Devise.omniauth_providers.each do |provider| %>
            <li>
            <%= link_to image_tag("#{provider.to_s}_64.png", alt: "#{provider.to_s.titleize}"),
              omniauth_authorize_path(User, provider),
              method: "post",
              title: "Sign-in with #{provider.to_s.titleize}"
              %>
            </li>
          <% end -%>
          </ul>
          </li>
        <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  <% if alert %>
    <div class="alert alert-danger alert-dismissible" role="alert"><%= alert %></div>
  <% end %>
  <% if notice %>
    <div class="alert alert-warning alert-dismissible" role="alert"><%= notice %></div>
  <% end %>
  <br />
  <br />
  <div class='container'>
    <div class='row'>
      <div class='col-sm-4'><%= image_tag 'logo.jpg' %></div>
      <div class='col-sm-8'><%= yield %></div>
    </div>
  </div>
<%#= console if Rails.env.development? %>
</body>
</html>
